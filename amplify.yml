version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd holosonic
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: holosonic/dist
    files:
      - '**/*'
  cache:
    paths:
      - holosonic/node_modules/**/*
